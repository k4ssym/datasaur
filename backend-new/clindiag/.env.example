# ── Qazcode LLM API ──────────────────────────────────────────────────────────
QAZCODE_BASE_URL=https://hub.qazcode.ai
QAZCODE_API_KEY=sk-your-key-here
QAZCODE_MODEL=oss-120b
LLM_TIMEOUT=60

# ── RAG ──────────────────────────────────────────────────────────────────────
# Path to FAISS index directory (build with: python build_index.py)
INDEX_DIR=./index
RAG_TOP_K=4

# ── Prompts ──────────────────────────────────────────────────────────────────
PROMPTS_FILE=./prompts.json

# ── HuggingFace cache (optional) ─────────────────────────────────────────────
HF_HOME=./.cache/huggingface

# ── Frontend (Next.js dev only) ───────────────────────────────────────────────
# In Docker, NEXT_PUBLIC_API_BASE="" so requests go to same origin (FastAPI)
# In dev, leave unset so the Next.js proxy route (/api/backend/*) is used
# NEXT_PUBLIC_API_BASE=
BACKEND_URL=http://localhost:8080
